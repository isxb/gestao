# gestao/public/.htaccess

<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Linha RewriteBase / Comentada ou Removida
    # RewriteBase / 

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [L]
</IfModule>